# ai-workbench

A structured workspace for AI image generation and local model tooling.

## Structure

- Docker services live under `docker/`
- Model weights and assets live under `models/`
- Shared scripts and templates live under `scripts/` and `templates/`
- Workspace and backup data live under `workspace/` and `backups/`
